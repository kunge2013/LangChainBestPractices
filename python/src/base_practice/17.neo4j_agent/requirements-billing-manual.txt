# Additional dependencies for billing manual agent (beyond requirements-neo4j-hfembdding.txt)
# The base requirements are already in requirements-neo4j-hfembdding.txt:
#   langchain-huggingface, langchain-neo4j, langchain-openai, deepagents, python-dotenv
#
# Only new packages not already in the base requirements:

python-docx>=1.1.0
Pillow>=10.0.0
