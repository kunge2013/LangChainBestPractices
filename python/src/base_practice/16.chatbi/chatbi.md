以下是基于我们之前讨论的 **7 阶段架构** 设计的 **完整 ChatBI 系统流程图与架构图**。它清晰展示了数据流、控制流、各层模块职责，以及关键存储组件（实体字典、本体库、映射表）的位置。

---

### 源码文件

![ChatBiAgentV1.py](ChatBiAgentV1.py)

### 🧭 完整架构流程图（含存储层）



```mermaid
flowchart TD
    %% 样式定义
    classDef user fill:#f5b7b1,stroke:#c0392b,stroke-width:2px;
    classDef nlp fill:#f9e79f,stroke:#b7950b,stroke-width:2px;
    classDef ontology fill:#d4a0f7,stroke:#7b2fbe,stroke-width:2px;
    classDef semantic fill:#87CEEB,stroke:#4682B4,stroke-width:2px;
    classDef engine fill:#a9dfbf,stroke:#1e8449,stroke-width:2px;
    classDef result fill:#f1948a,stroke:#922b21,stroke-width:2px;
    classDef storage fill:#ffe6cc,stroke:#d79b00,stroke-width:2px;
    classDef decision fill:#fcf3cf,stroke:#d4ac0d,stroke-width:2px;

    %% ==================== 用户输入层 ====================
    U["👤 用户输入<br>『一类城市上个月营收』"] --> N1

    %% ==================== 阶段2：增强型 NLU ====================
    subgraph Stage2["阶段2：增强型自然语言理解"]
        N1["步骤2.1：LLM 基础实体抽取"] --> N2{"步骤2.2：策略层<br>实体字典/模糊匹配"}
        N2 -->|精准匹配| N3a["直接通过"]
        N2 -->|模糊/错误| N3b["模糊匹配修正<br>（拼音/编辑距离纠偏）"]
        N2 -->|完全未知| N3c["挂起并反问"]
        N3a & N3b & N3c --> N4["步骤2.3：上下文增强<br>（RAG 检索背景知识）"]
        N4 --> N5["步骤2.4：输出结构化槽位<br>指标=营收，维度=城市，<br>时间=上个月，过滤=城市类型=一类"]
    end

    %% ==================== 阶段3：本体层 ====================
    subgraph Stage3["阶段3：本体层（业务推理与扩层）"]
        O1["步骤3.1：实体链接<br>将槽位映射到本体类"] --> O2{"步骤3.2：本体推理引擎<br>（子类/实例查询）"}
        O2 -->|推理| O3["步骤3.3：逻辑扩层<br>生成具体业务名单<br>（如 上海/北京/广州）"]
        O2 -.->|读取| OntoStore[("本体库<br>（图数据库/RDF）")]
        O3 --> O4["步骤3.4：输出客户ID列表<br>（物理标识）"]
    end

    N5 --> O1

    %% ==================== 阶段4：语义层（逻辑映射） ====================
    subgraph Stage4["阶段4：语义层（逻辑映射）"]
        S1["步骤4.1：指标映射<br>（指标→聚合函数）"] --> S2["步骤4.2：维度映射<br>（维度→逻辑字段名）"]
        S1 -.->|读取| MetricStore[("指标定义库")]
        S2 -.->|读取| DimStore[("维度定义库")]
        S2 --> S3["步骤4.3：组装逻辑SQL<br>（使用业务名）"]
    end

    O4 --> S1
    O4 --> S2

    %% ==================== 阶段5：物理值映射 ====================
    subgraph Stage5["阶段5：物理值映射（语义层内部）"]
        M1["步骤5.1：查询映射字典"] --> M2["步骤5.2：获取物理编码<br>（城市→编码，客户名→ID）"]
        M2 -.->|读取| MapStore[("映射表<br>（配置文件/DB/缓存）")]
        M2 --> M3["步骤5.3：替换逻辑SQL中的业务名<br>生成物理SQL中间态"]
    end

    S3 --> M1

    %% ==================== 阶段6：SQL编译与执行 ====================
    subgraph Stage6["阶段6：SQL编译与执行引擎"]
        E1["步骤6.1：方言编译器<br>（适配ClickHouse/Snowflake等）"] --> E2["步骤6.2：生成最终可执行SQL"]
        E2 --> E3["步骤6.3：提交至数据仓库执行"]
    end

    M3 --> E1

    %% ==================== 阶段7：结果校验与返回 ====================
    subgraph Stage7["阶段7：结果校验与返回"]
        R1["步骤7.1：获取原始查询结果"] --> R2{"步骤7.2：结果自洽性校验<br>（本体规则）"}
        R2 -->|校验通过| R3["步骤7.3：格式化输出"]
        R3 --> R4["步骤7.4：返回自然语言答案"]
        R2 -->|校验失败| R_ERR["步骤7.5：触发告警/重试<br>或提示用户"]
        R2 -.->|读取规则| OntoStore
    end

    E3 --> R1

    %% ==================== 存储层汇总 ====================
    subgraph Storage["存储层（元数据 & 物理数据）"]
        direction LR
        OntoStore --- MapStore
        MapStore --- MetricStore
        MetricStore --- DimStore
        DimStore --- DB[(数据仓库<br>（SQLite/ClickHouse）)]
    end

    %% 连接关系
    E3 -.->|读写数据| DB
    R1 -.->|返回数据| DB

    %% ==================== 样式应用 ====================
    class U,N1,N4,N5 user;
    class N2,N3a,N3b,N3c decision;
    class O1,O2,O3,O4,OntoStore ontology;
    class S1,S2,S3,M1,M2,M3,MetricStore,DimStore,MapStore semantic;
    class E1,E2,E3 engine;
    class R1,R2,R3,R4,R_ERR result;
    class DB storage;
```

---

### 📐 架构分层与职责总览

| 层级 | 模块 | 核心职责 | 输入 | 输出 | 关键存储依赖 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **用户层** | 用户输入 | 发起自然语言查询 | 文本问题 | 原始查询字符串 | — |
| **增强型 NLU** | 实体抽取 + 策略修正 | 提取结构化槽位，利用字典/模糊匹配纠偏，RAG 增强 | 文本 | 实体（时间、地点、客户、指标） | 实体字典 |
| **本体层** | 实体链接 + 推理引擎 | 将概念链接到本体，推理子类/实例，扩展为具体物理 ID 列表 | 槽位 | 客户 ID 列表 | 本体库（图结构） |
| **语义层（逻辑）** | 指标/维度映射 + 逻辑SQL组装 | 将业务指标和维度映射为逻辑表达式和字段名，生成带业务名的 SQL | 实体 + 客户ID | 逻辑 SQL（含业务名） | 指标定义库、维度定义库 |
| **语义层（物理）** | 物理值映射 | 将逻辑 SQL 中的业务名替换为物理编码（城市码、客户ID、字段名） | 逻辑 SQL | 物理 SQL（含编码） | 映射表（配置文件/DB/缓存） |
| **执行引擎** | SQL 编译 + 方言适配 + 提交执行 | 将 SQL 转换为目标数据库方言并执行 | 物理 SQL | 原始结果（数值） | 数据仓库 |
| **校验与返回** | 结果自洽性校验 + 格式化 | 利用本体规则检查结果是否合理，格式化后返回 | 原始结果 | 自然语言答案 | 本体库（规则） |

---

### 🔄 完整数据流（文字描述）

1. **用户输入** `"一类城市上个月营收"`。
2. **NLU** 抽取出 `{时间=上个月, 地点=一类城市, 指标=营收}`，并经过 **策略修正**（若“一类城市”未命中，模糊匹配到具体城市）。
3. **本体层** 将“一类城市”链接到本体中的类，推理出**实例列表**（如上海、北京、广州），再转换为对应的**物理客户 ID 列表**（例如 `['CUST_SH_001', 'CUST_BJ_002']`）。
4. **逻辑映射** 将“营收”映射为 `SUM(balance)`，将“城市”映射为逻辑字段 `city_name`，组装成**逻辑 SQL**：`SELECT SUM(balance) FROM fact WHERE customer_id IN (...) AND city_name = '一类城市' AND date BETWEEN ...`。
5. **物理映射** 将 `city_name = '一类城市'` 替换为 `city_code IN ('021','010','020')`，同时客户 ID 已为物理 ID，生成**物理 SQL**。
6. **执行引擎** 适配方言（如 ClickHouse）后提交给数据仓库，返回数值。
7. **校验层** 检查数值是否合理（如应收款 ≥ 0），通过则格式化返回；否则告警。

---

### 💎 关键设计要点

- **职责单一**：每个阶段独立工具，便于测试、替换和扩展。
- **策略层前置**：在 NLU 阶段引入字典和模糊匹配，显著提升实体解析准确率。
- **本体与语义层分离**：本体只处理逻辑推理（扩层），不触碰物理存储细节；语义层负责所有映射。
- **校验后置**：利用本体规则进行最终把关，保障结果质量。
- **存储解耦**：映射表、本体库、指标定义均可独立维护，支持热更新。

此架构可直接应用于生产级 ChatBI 系统，并可根据业务需求灵活扩展（如增加更多指标、维度、本体关系等）。